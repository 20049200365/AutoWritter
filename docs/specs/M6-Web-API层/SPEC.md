# M6 Web API 层 — 模块 Spec

> 状态：待编写 ｜ 依赖：M1~M5 的路由 ｜ 被依赖：M7
> 职责：REST 路由装配、本机鉴权、错误规范、OpenAPI 文档、静态托管、启动入口
> 对外契约：◆ OpenAPI（架构 SPEC §8 的路由清单为底稿）

## 1. 目标与边界
（待编写）

## 2. 路由装配
（待编写：架构 SPEC §8.1 路由清单的资源归属、分页/过滤约定）

## 3. 通用规范
（待编写：错误码、鉴权（本机 token）、SSE 端点挂载方式）

## 4. 部署与启动
（待编写：启动脚本、数据目录、配置项）

## 5. 验收标准
（待编写）
