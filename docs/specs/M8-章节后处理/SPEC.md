# M8 章节后处理 — 模块 Spec

> 状态：待编写 ｜ 依赖：M1 M2 M3 ｜ 被依赖：无（产物经 M6 暴露给前端）
> 职责：架构 SPEC §5.4 流水线第 2~8 步：摘要生成、实体抽取、关系/伏笔/时间线建议、大纲对账、建议消息
> 对外契约：◆ 建议消息模型（属 M1，suggestion 标记的对话消息）+ 队列任务接口

## 1. 目标与边界
（待编写）

## 2. 后处理流水线
（待编写：8 步流水线的任务编排、失败重试、进度上报）

## 3. 抽取与建议机制
（待编写：实体抽取、关系变更、伏笔侦测、时间线事件的提示词契约与建议消息流转）

## 4. 大纲对账
（待编写：正文与 L3 节拍偏差的检测口径与提示方式）

## 5. 验收标准
（待编写）
