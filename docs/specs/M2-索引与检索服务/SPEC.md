# M2 索引与检索服务 — 模块 Spec

> 状态：待编写 ｜ 依赖：M1 ｜ 被依赖：M3 M8
> 职责：切块、jieba/FTS5 索引、实体共现索引、实体+关键词检索、LLM 查询改写、分层摘要维护、重建索引
> 对外契约：◆ `SearchService.search(query, filters, k)` / `index_source(...)`

## 1. 目标与边界
（待编写）

## 2. 索引设计
（待编写：切块策略、FTS5 影子表、chunk_entities 共现表、增量与重建）

## 3. 检索算法
（待编写：架构 SPEC §5.5 落地——实体路/关键词路/LLM 查询改写、排序融合）

## 4. 分层摘要维护
（待编写：章/卷/全局摘要的生成时机与刷新策略）

## 5. 验收标准
（待编写）
